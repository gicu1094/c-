#include <string>

std::string fakeBin(std::string str){

  
  for(int i=0; i<str.size(); ++i)
  {
      if(str[i]-53 >= 0)
      {
        str[i]='1';
      }
      else
      {
        str[i] = '0';
      }
  }
  return str;
}
